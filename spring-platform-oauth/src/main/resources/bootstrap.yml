spring:
  application:
    name: PLATFORM-OAUTH
  profiles:
    active: mysql,redis,admin
  cloud:
    nacos:
      discovery:
        server-addr: 42.192.99.152:8848
server:
  port: 5203
  servlet:
    context-path: /platform-oauth
  file:
    path: /root/serviceLogs/
customer:
  feign:
    client-id: Admin
    client-secret: Admin

